{
  "name": "unb-site",
  "private": true,
  "version": "1.0.0",
  "description": "unbgroup.uz — static site. Node is only used to build the HTML decks from the source PDFs.",
  "type": "module",
  "scripts": {
    "build": "npm run build:pages && npm run build:sitemap",
    "build:decks": "node tools/build-decks.mjs",
    "build:pages": "node tools/build-pages.mjs",
    "build:sitemap": "node tools/build-sitemap.mjs"
  },
  "devDependencies": {
    "mupdf": "^1.28.0",
    "node-html-parser": "^9.0.2",
    "sharp": "^0.34.5"
  }
}
